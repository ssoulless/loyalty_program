<%= form_for(@transaction) do |f| %>
  <% if @transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transaction.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
      <% @transaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :total %><br>
    <%= f.text_field :total %>
  </div>
  <div class="field">
    <%= f.label :points_awarded %><br>
    <%= f.text_field :points_awarded %>
  </div>
  <%= hidden_field_tag 'store_id', params[:store_id] %>
  <%= hidden_field_tag 'client_document', params[:client_document] %>
  <div class="actions">
    <%= f.submit 'Submit', class: "button small-6 columns expand" %>
  </div>
<% end %>
