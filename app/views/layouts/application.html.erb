<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag "vendor/modernizr" %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render partial: 'layouts/flash' %>
    <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to "Leal program", root_path %></h1>
        </li>
         <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
        <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
      </ul>

      <section class="top-bar-section">
        <% if user_signed_in? %>
          <!-- Right Nav Section -->
          <ul class="right">
            <li><%= link_to %Q|Log out|, destroy_user_session_path, :method => 'delete', class: "" %></li>
          </ul>
          <!-- Left Nav Section -->
          <ul class="left">
            <li><%= link_to "Stores", root_path %></li>
            <li><%= link_to "Top loyal customers", clients_path %></li>
          </ul>
        <% end %>
      </section>
    </nav>
    
    <div class="row collapse principal">
      <div class="small-12 small-centered medium-6 medium-centered large-6 large-centered columns">
        <%= yield %>
      </div>
    </div>

  </body>
</html>
